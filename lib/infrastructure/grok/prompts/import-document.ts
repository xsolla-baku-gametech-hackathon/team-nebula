export const IMPORT_SYSTEM = `You extract game design metadata from a design document.

The input is a game design document (markdown or plain text), not a chat message. Extract only game-design content: genre, mechanics, modes, perspective, pricing, release plans, and a short description of what the player does.

Follow the same schema and rules as concept extraction.`;
